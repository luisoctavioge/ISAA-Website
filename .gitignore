# ⚠ Material interno — este repositorio es PÚBLICO
# El manual de marca está marcado "Uso interno v2.0": contiene
# posicionamiento competitivo, estructura de precios con márgenes
# y messaging por audiencia. No entra al repo, nunca.
*.pdf
*.zip
/docs/internal/

# Carpeta de entrega · se genera desde docs/, no se versiona
/Para-Jesus/

# Salida de build/inline.mjs · se regenera, no se versiona
/build/inline/

# Fuente original de un solo archivo, ya refactorizada a
# index.html + assets/. Se conserva local como respaldo.
isaa-home.html

# Tipografías. Nulshock es de pago y este repo es PÚBLICO: solo entra la
# que el sitio necesita servir, y la licencia corre por cuenta de Jesús
# (decisión suya, 24 de septiembre de 2026). Cualquier otra, fuera.
*.otf
*.ttf
*.woff
*.woff2
!assets/fonts/nulshock-bd.otf
!assets/fonts/nulshock-bd.woff2

# Sistema operativo / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Por si acaso
node_modules/
.env
.env.*
*.key
*.pem

# Documento de trabajo interno — no es contenido del sitio
handoff-claude-code.md

# Masters de video sin comprimir — el repo es público y git guarda
# cada versión de un binario para siempre. Solo entra el comprimido.
assets/video/TEST-*
assets/video/*-master.*
assets/video/*.mov
